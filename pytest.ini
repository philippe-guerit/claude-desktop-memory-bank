[pytest]
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function

# Filter warnings
filterwarnings =
    # Ignore "coroutine 'main' was never awaited" warning
    ignore:coroutine 'main' was never awaited:RuntimeWarning
    # Add other warnings to ignore as needed
